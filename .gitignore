# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/

# Node
node_modules/
dist/
build/
.vite/

# Environment
.env
*.local

# Editor
.DS_Store
.vscode/
.idea/

# AI coding tool local working dirs (session/config data, not project source).
# NOTE: this does NOT affect .ai-conversations/ below — the pre-commit hook copies
# captured conversation content there regardless of these dirs' tracked status.
.claude/
.cursor/
.aider*

# AI conversations captured by pre-commit hook
# (intentionally not ignored — they are part of the evaluation)
